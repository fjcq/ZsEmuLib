/* 帮助系统正文：常见问题解答 */

window.LS_DOC = window.LS_DOC || [];
window.LS_DOC.push({
  id: 'faq',
  title: '常见问题解答',
  keys: '常见问题 FAQ 错误 排错 失败 返回空 超时 单位 死锁 乱码 权限 端口占用 索引 异步 卡死 管理员',
  html: [
    '<h1>常见问题解答</h1>',
    '<p class="lead">以下问题按“现象 → 原因 → 解决”整理，全部来自模块源码注释与真实使用场景。' +
    '遇到报错时建议先对照本文定位，再用 <code class="inline">自定义命令()</code> 验证底层命令行是否正常。</p>',

    '<h2>一、返回值与判断类问题</h2>',

    '<h3>Q1：命令返回了空文本，是成功还是失败？</h3>',
    '<p><b>现象：</b>执行 <code class="inline">adb.重载文件系统 (0)</code>、' +
    '<code class="inline">lsc.关闭模拟器 (0)</code> 等命令后，返回文本是空的，' +
    '不确定该按成功还是失败处理。</p>',
    '<p><b>原因：</b>模块中大量文本型返回值遵循“<b>空文本＝成功，非空文本＝错误信息</b>”的约定，' +
    '这与很多人的直觉相反。</p>',
    '<p><b>解决：</b>判断条件写成等于空文本。</p>',
    EPL.block([
      '.局部变量 结果, 文本型',
      '',
      '结果＝adb.重载文件系统 (0)',
      '.如果 (结果 ＝ “”)',
      '    输出调试文本 (“重载成功”)',
      '.否则',
      '    输出调试文本 (“重载失败：” + 结果)',
      '.如果结束'
    ].join('\n'), '空文本即成功'),

    '<h3>Q2：<code class="inline">返回：无返回值</code> 的命令怎么知道有没有成功？</h3>',
    '<p><b>原因：</b>例如 <code class="inline">雷神_结束进程 ()</code>、' +
    '<code class="inline">雷神_安装虚拟机 ()</code>、' +
    '<code class="inline">雷神_调整窗口 ()</code> 中的前两个只做动作不反馈结果。</p>',
    '<p><b>解决：</b>用完后再查一次状态来确认。例如结束进程后用 ' +
    '<code class="inline">雷神_取运行中模拟器目录 ()</code> 或 ' +
    '<code class="inline">adb.枚举设备 ()</code> 检查；调整窗口后调用 ' +
    '<code class="inline">雷神_获取窗口位置和尺寸 ()</code> 复核。</p>',

    '<h2>二、环境与安装目录类问题</h2>',

    '<h3>Q3：<code class="inline">雷神_取安装目录 ()</code> 返回空文本</h3>',
    '<p><b>原因：</b>该命令的探测顺序是：模块缓存的 <b>全_工作目录</b> → 运行中的模拟器进程 → ' +
    '注册表（按雷电 14 / 雷电 9 / 雷电 5 / 雷神 64 / 雷神 … 的优先级）→ 桌面快捷方式。' +
    '全都失败时返回空文本。</p>',
    '<p><b>解决：</b></p>',
    '<ol>',
    '<li>确认模拟器确实已安装（至少存在 <code class="inline">lsplayer.exe</code> 或 ' +
    '<code class="inline">dnplayer.exe</code>）。</li>',
    '<li>安装目录被手工移动过时，注册表仍指向旧路径。此时直接手工指定：' +
    '<code class="inline">雷神_置安装目录 (“E:\\模拟器\\LDPlayer9\\”, 真)</code>，' +
    '第 2 个参数为真会把正确目录写回注册表。</li>',
    '<li>需要一次性列出本机全部模拟器时，用 ' +
    '<code class="inline">雷神_取安装目录_全部 (目录数组)</code>，它支持枚举雷电、雷神与 MuMu。</li>',
    '</ol>',

    '<h3>Q4：<code class="inline">启动模拟器 ()</code> 一直返回假</h3>',
    '<p><b>原因 1：超时时间单位填错。</b><code class="inline">雷神lsconsole操作.启动模拟器 ()</code> ' +
    '的第 3 个参数单位是<b>秒</b>；而 <code class="inline">新建模拟器 ()</code>、' +
    '<code class="inline">克隆模拟器 ()</code> 的超时参数单位是<b>毫秒</b>。</p>',
    '<p><b>原因 2：模拟器版本过低。</b>lsconsole 的相关命令普遍要求雷电/雷神 2.0 以上；' +
    '<code class="inline">取模拟器信息EX ()</code> 需要 2.0.31+，' +
    '<code class="inline">修改标题 ()</code> 需要 2.0.33+。</p>',
    '<p><b>原因 3：没有先指定安装目录。</b>使用雷神模拟器时，' +
    '<b>必须先执行 <code class="inline">置模拟器目录 ()</code></b>，否则内部找不到 ' +
    '<code class="inline">lsconsole.exe</code>。</p>',
    '<p><b>解决：</b>按 <a href="#/quickstart">快速入门</a> 中的顺序调用，' +
    '并把超时时间按“秒”填写；启动后务必用 <code class="inline">adb.等待连接 ()</code> ' +
    '等待安卓系统真正就绪。</p>',

    '<h3>Q5：模拟器能启动，但 adb 一直连不上</h3>',
    '<p><b>原因：</b>adb 端口被占用、adb 服务状态异常，或模拟器的 adb 版本与指定版本不一致。</p>',
    '<p><b>解决：</b>按下面的顺序做“三步复位”，模块自带的快捷键就是这么做的。</p>',
    EPL.block([
      '.子程序 修复adb连接, 逻辑型',
      '.参数 索引, 整数型',
      '',
      '输出调试文本 (adb.枚举设备 ())          ' + "' 先看当前设备列表",
      'adb.停止服务 ()                          ' + "' 停止 adb 服务",
      'adb.强制释放端口 (索引, 假)             ' + "' 释放该模拟器占用的 TCP 端口（第2参数为真时连虚拟机进程一起结束）",
      'adb.启动服务 ()                          ' + "' 重新启动 adb 服务",
      '输出调试文本 (adb.枚举设备 ())',
      '',
      "' 最后阻塞等待连接，成功返回空文本（第 2 个参数单位：毫秒）",
      '.如果真 (adb.等待连接 (索引, 60000) ＝ “”)',
      '    返回 (真)',
      '.如果真结束',
      '返回 (假)'
    ].join('\n'), 'ADB 连接三步复位'),

    '<h3>Q6：adb 版本不匹配 / 想用自己的 adb</h3>',
    '<p><b>解决：</b>用 <code class="inline">指定adb ()</code> 显式指定，留空则恢复为模拟器目录中的 adb.exe。</p>',
    EPL.block([
      '.子程序 换成自己的adb',
      '.局部变量 版本号, 文本型',
      '.局部变量 内部版本号, 文本型',
      '',
      '.如果真 (adb.指定adb (“D:\\platform-tools\\adb.exe”) ＝ 假)',
      '    输出调试文本 (“指定 adb 失败”)',
      '    返回 ()',
      '.如果真结束',
      '',
      "' 查看当前正在使用的 adb 完整路径（后两个参数回传版本号）",
      '输出调试文本 (adb.取adb文件路径 (版本号, 内部版本号))',
      '输出调试文本 (“adb 版本：” + 版本号 + “.” + 内部版本号)',
      '输出调试文本 (adb.取adb版本 ())'
    ].join('\n'), '指定自定义 adb'),

    '<h2>三、配置与文件类问题</h2>',

    '<h3>Q7：修改了分辨率 / CPU / 内存，为什么没生效？</h3>',
    '<p><b>原因：</b><code class="inline">修改配置 ()</code>、' +
    '<code class="inline">雷神_修改配置 ()</code>、' +
    '<code class="inline">雷神_设置窗口位置和尺寸 ()</code> 都是写模拟器配置文件，' +
    '<b>必须在模拟器关闭状态下修改</b>，且多数改动要下次启动才生效。</p>',
    '<p><b>解决：</b>先关闭模拟器，再修改，最后重新启动。</p>',
    EPL.block([
      "' ① 先关闭",
      'lsc.关闭模拟器 (0)',
      '',
      "' ② 再写配置（属性名可直接用模块常量）",
      '雷神_修改配置 (0, #配置项_分辨率_宽度, “1280”, 2)',
      '雷神_修改配置 (0, #配置项_分辨率_高度, “720”, 2)',
      '雷神_修改配置 (0, #配置项_ROOT权限, “true”, 3)',
      '',
      "' ③ 启动后生效",
      'lsc.启动模拟器 (0, , 120)'
    ].join('\n'), '关闭状态下改配置'),
    '<div class="danger"><b>谨慎使用：</b><code class="inline">雷神_修改配置 ()</code> 直接改写 ' +
    '<code class="inline">leidian.config</code>。写入错误内容可能损坏配置文件，' +
    '建议先用 <code class="inline">雷神_备份配置文件 (0, “E:\\bak\\vm0.config”)</code> 做备份。</div>',

    '<h3>Q8：上传 / 下载文件失败，或者写入系统目录失败</h3>',
    '<p><b>原因：</b>安卓系统目录默认是只读挂载；部分目录还需要 ROOT 权限。</p>',
    '<p><b>解决：</b>按“ROOT → 重载文件系统 → 再上传”的顺序操作。</p>',
    EPL.block([
      '.子程序 上传到系统目录',
      '.局部变量 结果, 文本型',
      '',
      "' ① 确认已 ROOT（未开启时先开启）",
      '.如果真 (adb.是否root (0) ＝ 假)',
      '    结果＝adb.root (0)',
      '    .如果真 (结果 ≠ “”)',
      '        输出调试文本 (“ROOT 失败：” + 结果)',
      '        返回 ()',
      '    .如果真结束',
      '.如果真结束',
      '',
      "' ② 把 /system 重新挂载为可写",
      '结果＝adb.重载文件系统 (0)',
      '.如果真 (结果 ≠ “”)',
      '    输出调试文本 (“重载失败：” + 结果)',
      '    返回 ()',
      '.如果真结束',
      '',
      "' ③ 再上传",
      '.如果真 (adb.上传文件 (0, “E:\\下载\\lib.so”, “/system/lib/lib.so”) ＝ 假)',
      '    输出调试文本 (“上传失败”)',
      '.如果真结束'
    ].join('\n'), 'ROOT + 重载文件系统 + 上传'),
    '<div class="tip"><b>小技巧：</b>如果只是传普通文件，优先传到<b>共享目录</b>' +
    '（<code class="inline">雷神_取共享目录 (0, 1)</code> 取回电脑侧路径），' +
    '模拟器内对应的路径是 <code class="inline">/sdcard/Pictures/</code>，无需 ROOT，速度也更快。' +
    '另外下载目录时，电脑侧路径<b>尽量不要包含中文或特殊字符</b>。</div>',

    '<h3>Q9：<code class="inline">写文本 ()</code> 在雷电自带 adb 上失败</h3>',
    '<p><b>原因：</b>源码注释明确指出：雷电自带的 adb 无法通过该方式成功写出文件。</p>',
    '<p><b>解决：</b>改用 <code class="inline">雷神ls操作</code> 中的 <code class="inline">写文本 ()</code>，' +
    '或先 <code class="inline">指定adb ()</code> 换成官方 platform-tools 中的 adb。</p>',

    '<h3>Q10：读回来的文本是乱码</h3>',
    '<p><b>原因：</b>安卓侧输出可能是 UTF-8，而易语言文本型默认按 ANSI 处理。</p>',
    '<p><b>解决：</b>用 <code class="inline">类_编码转换</code> 显式转换；' +
    '二进制内容请改用带 <code class="inline">_字节集</code> 后缀的命令。</p>',
    EPL.block([
      '.局部变量 编码转换, 类_编码转换',
      '.局部变量 原始, 字节集',
      '',
      "' 文本型命令返回乱码时，先转成字节集再按 UTF-8 解码",
      '原始＝到字节集 (adb.自定义命令 (“shell getprop ro.product.model”))',
      '输出调试文本 (编码转换.UTF8到文本 (原始))',
      '',
      "' 需要写入 UTF-8 文件时反向转换",
      '写到文件 (“E:\\out.txt”, 编码转换.文本到UTF8 (“中文内容”, 真))'
    ].join('\n'), '编码转换'),

    '<h2>四、多线程与稳定性问题</h2>',

    '<h3>Q11：界面卡死、按钮点不动</h3>',
    '<p><b>原因：</b>模块的很多命令是<b>阻塞式</b>的（内部要启动进程并等待输出），' +
    '例如 <code class="inline">等待连接 ()</code>、<code class="inline">启动模拟器 ()</code>、' +
    '<code class="inline">枚举安卓控件 ()</code>。在 UI 线程直接调用就会冻结窗口。' +
    '源码中也特别提示：<code class="inline">雷神_取运行中模拟器目录 ()</code> ' +
    '<b>建议不要在主线程中执行</b>，会导致 UI 阻塞。</p>',
    '<p><b>解决：</b>把耗时的模拟器命令放进线程，UI 更新用子类化消息回传（模块自带调试窗口即采用该方案）。</p>',
    EPL.block([
      '.子程序 _按钮_启动_被单击',
      '',
      "' 只负责起线程，立刻返回，界面不卡",
      '启动线程 (&线程_启动模拟器, 0, )',
      '',
      '.子程序 线程_启动模拟器',
      '.参数 索引, 整数型',
      '',
      '.如果真 (lsc.启动模拟器 (索引, , 120) ＝ 假)',
      '    输出调试文本 (“启动失败或超时”)',
      '    返回 ()',
      '.如果真结束',
      "' 等待连接：第 2 个参数是毫秒，60000＝60 秒",
      '输出调试文本 (adb.等待连接 (索引, 60000))'
    ].join('\n'), '耗时命令放入线程'),

    '<h3>Q12：用了 <code class="inline">类_高性能读写锁</code> 之后线程卡死</h3>',
    '<p><b>原因：</b>源码中的“防呆说明”指出：<b>同一线程、同一个锁对象</b>，' +
    '在【进入读锁】后、未【退出读锁】之前调用【进入写锁】，会造成<b>死锁</b>；' +
    '读锁状态下只允许再次进入读锁。写锁状态下可以再次进入写锁或读锁，' +
    '但<b>进入与退出必须成对</b>。</p>',
    '<p><b>解决：</b></p>',
    '<ul>',
    '<li>保证 <code class="inline">进入读锁/写锁</code> 与 ' +
    '<code class="inline">退出读锁/写锁</code> 严格成对，异常分支也要退出。</li>',
    '<li>不要把读锁与写锁嵌套在同一线程的同一次调用链里。</li>',
    '<li>只用作互斥时，可以统一使用写锁（此时等价于一个高性能许可区）。</li>',
    '</ul>',

    '<h3>Q13：管道类（<code class="inline">类_命名管道</code> / <code class="inline">类_匿名管道</code>）取不到进程 ID 或退出代码</h3>',
    '<p><b>原因：</b><code class="inline">停止 ()</code> 会关闭全部管道句柄、关闭进程句柄，' +
    '并把进程 ID、线程 ID 清零。因此 <b>在停止之后再取就是无效值</b>。</p>',
    '<p><b>解决：</b>严格按“启动 → 读/写 → 取退出代码 → 停止”的顺序调用。</p>',
    EPL.block([
      '.局部变量 管道, 类_命名管道',
      '.局部变量 数据, 字节集',
      '.局部变量 退出代码, 整数型',
      '',
      '.如果真 (管道.启动 (“cmd.exe”, ) ＝ 0)',
      '    返回 ()',
      '.如果真结束',
      '',
      "' 写命令时，要执行命令行请在结尾加上一个换行符",
      '管道.写 (“echo hello” + #换行符)',
      '',
      "' 读：超时时间 ≤ 0 表示一直等到读到数据且暂无后续数据为止",
      '数据＝管道.读 (, 5000, )',
      '输出调试文本 (到文本 (数据))',
      '',
      "' 关键：退出代码必须在 停止() 之前取",
      '退出代码＝管道.取退出代码 ()',
      '输出调试文本 (“退出代码：” + 到文本 (退出代码))',
      '',
      '管道.停止 ()'
    ].join('\n'), '管道类的正确调用顺序'),
    '<div class="warn"><b>补充：</b><code class="inline">读 ()</code> 在没有数据时会阻塞。' +
    '如果不想长时间等待，务必传入合适的超时时间；另外管道类实例也不要跨线程共享，' +
    '每个线程使用自己的实例。</div>',

    '<h2>五、MuMu 相关</h2>',

    '<h3>Q14：MuMu 的命令全部失败，提示版本不支持</h3>',
    '<p><b>原因：</b><code class="inline">MuMu管理器</code> 的绝大多数接口（尤其是所有配置、' +
    '性能、工具栏相关命令）都<b>要求 MuMu 4.0 及以上版本</b>；' +
    '<code class="inline">版本 ()</code> 甚至要求 5.0 以上。</p>',
    '<p><b>解决：</b>先在模拟器侧升级到 MuMu 12（4.0.0+）或 MuMu 5（5.0.0+），' +
    '并确认工作目录正确：</p>',
    EPL.block([
      '.局部变量 结果, 文本型',
      '',
      "' 工作目录即 mumu-cli.exe（旧版为 MuMuManager.exe）所在目录",
      '结果＝M_取工作目录 (#版本_Mu)',
      '.如果真 (结果 ＝ “”)',
      '    信息框 (“未找到 MuMu 工作目录”, 0, )',
      '    返回 ()',
      '.如果真结束',
      'mu.置工作目录 (结果)',
      '',
      "' 也可直接查看当前使用的可执行文件路径",
      '输出调试文本 (mu.取执行程序 ())',
      '输出调试文本 (mu.帮助 (“”, 假))'
    ].join('\n'), 'MuMu 工作目录'),

    '<h3>Q15：<code class="inline">增强_</code> 系列返回失败 / 之后所有增强接口都失效</h3>',
    '<p><b>原因：</b>“增强_”系列是渲染层直连，必须先 <code class="inline">增强_连接 ()</code> ' +
    '拿到连接句柄，后续接口都要把这个句柄作为第 1 个参数传入；' +
    '截图类的接口还需要先取到应用的“显示标识”。</p>',
    '<p><b>解决：</b>按“连接 → 取显示标识 → 截图/触控 → 断开”的顺序调用，' +
    '并检查每一步的返回值：<code class="inline">增强_连接</code> 需 <b>&gt; 0</b>，' +
    '<code class="inline">增强_取显示标识</code> 需 <b>≥ 0</b>。</p>',
    EPL.block([
      '.局部变量 索引, 文本型',
      '.局部变量 句柄, 整数型',
      '.局部变量 标识, 整数型',
      '.局部变量 图片, 字节集',
      '',
      "' MuMu 的索引是“通用型（参考）”，必须先放到变量里再传入",
      '索引＝“0”',
      '句柄＝mu.增强_连接 (索引)',
      '.如果真 (句柄 ≤ 0)',
      '    输出调试文本 (“增强连接失败，句柄：” + 到文本 (句柄))',
      '    返回 ()',
      '.如果真结束',
      '',
      "' 取不到显示标识时，多半是应用包名写错或应用未运行",
      '标识＝mu.增强_取显示标识 (句柄, “com.tencent.mm”, )',
      '.如果真 (标识 ≥ 0)',
      '    图片＝mu.增强_截图 (句柄, 标识, , , , )   ' + "' 返回 BMP 字节集",
      '.如果真结束',
      '',
      "' 必须配对断开，否则句柄泄漏",
      'mu.增强_断开 (句柄)'
    ].join('\n'), 'MuMu 增强系列的正确用法'),

    '<h2>六、其它易错点速查</h2>',
    '<table>',
    '<tr><th style="width:30%">现象</th><th>原因与处理</th></tr>',
    '<tr><td>索引填 1 却控制了第二个模拟器</td>' +
    '<td>索引<b>从 0 开始</b>，第一个模拟器是 0，第二个才是 1。</td></tr>',
    '<tr><td>MuMu 想一次操作多个模拟器</td>' +
    '<td>MuMu 的索引参数是「通用型（参考）」，需先放变量再传入；支持 ' +
    '<code class="inline">“0,2,4”</code>（批量）与 ' +
    '<code class="inline">“all”</code>（全部，留空也等于全部）两种文本写法。</td></tr>',
    '<tr><td><code class="inline">取模拟器数量 ()</code> 在雷电上不可用</td>' +
    '<td>这是雷神专有命令。雷电请改用 <code class="inline">取全部标题 ()</code> 或 ' +
    '<code class="inline">取全部模拟器EX ()</code>。</td></tr>',
    '<tr><td><code class="inline">lsc.安装应用 ()</code> 还没装完就返回了</td>' +
    '<td>该命令是<b>异步</b>的，执行后立即返回。需要同步请改用 ' +
    '<code class="inline">adb.安装应用 ()</code>（会等待结果）。</td></tr>',
    '<tr><td>取不到进程 PID，返回全是 0</td>' +
    '<td><code class="inline">雷神_取PID ()</code> 需要<b>管理员权限</b>运行才能取到。</td></tr>',
    '<tr><td>用 <code class="inline">正则表达式类</code> 报错或匹配不到</td>' +
    '<td>该类<b>需要自行初始化 COM 库</b>（详见类源码说明），并且同一线程内使用。</td></tr>',
    '<tr><td><code class="inline">类_数组</code> 放整数型成员出错</td>' +
    '<td>该类<b>仅支持文本型与字节集型</b>，其他类型请使用易语言自带数组。</td></tr>',
    '<tr><td>调用参数想留空但编译不过</td>' +
    '<td>可空参数用两个连续逗号占位，例如 <code class="inline">启动模拟器 (0, , 120)</code>。' +
    '末尾的可空参数可以直接省略。</td></tr>',
    '<tr><td>Powershell 场景下命令报“无法识别”</td>' +
    '<td>把默认终端改为 PowerShell 后，命令写法需符合 PowerShell 语法；' +
    '不确定时可先用 <code class="inline">取终端路径 (“cmd.exe”)</code> 切回 cmd 验证。</td></tr>',
    '<tr><td>命令偶发失败但重试就好</td>' +
    '<td>删除用户、卸载等操作会有异步清理过程（源码注释已说明），' +
    '建议在关键命令后加 <code class="inline">延时 (500)</code> 或轮询状态，而不是立即重试。</td></tr>',
    '</table>',

    '<div class="tip"><b>还没解决？</b>请到 ' +
    '<a href="https://gitee.com/fjcq/ZsEmuLib/issues" target="_blank" rel="noopener">Gitee Issues</a> ' +
    '提交问题，并附上：完整的错误信息、操作系统版本、模拟器类型与版本、复现步骤。</div>'
  ].join('\n')
});
