/* 帮助系统正文：高级应用技巧 */

window.LS_DOC = window.LS_DOC || [];
window.LS_DOC.push({
  id: 'advanced',
  title: '高级应用技巧',
  keys: '高级 技巧 最佳实践 性能 优化 多线程 线程 读写锁 死锁 管道 命名管道 匿名管道 终端 超时 即时回显 自定义命令 批量 索引 哈希表 队列 数组 编码 截图 资源回收 稳定性',
  html: [
    '<h1>高级应用技巧</h1>',
    '<p class="lead">本章面向已经跑通 <a href="#/quickstart">快速入门</a> 的读者，' +
    '内容全部来自模块源码中的实现细节与注释：多线程模型、锁与管道、终端层调优、批量操作、' +
    '容器类参数取舍、编码处理以及资源回收清单。</p>',

    '<h2>一、线程模型：把阻塞命令移出 UI 线程</h2>',
    '<p>模块内部所有命令最终都是“创建子进程 → 等待输出 → 解析文本”，因此' +
    '<b>大量接口是阻塞式的</b>，例如 <code class="inline">等待连接 ()</code>、' +
    '<code class="inline">启动模拟器 ()</code>、<code class="inline">枚举安卓控件 ()</code>、' +
    '<code class="inline">雷神_取运行中模拟器目录 ()</code>。在易语言主线程（UI 线程）里直接调用，' +
    '窗口就会卡住不动。模块源码在 <code class="inline">窗口_模块调试</code> 中也写明：' +
    '“建议这个子程序不要在主线程中执行，会导致 UI 阻塞”。</p>',
    '<h3>1. 标准做法：启动线程 + 子线程内完成整条流程</h3>',
    EPL.block([
      '.版本 2',
      '',
      '.程序集 窗口程序集_启动窗口',
      '.程序集变量 adb, 雷神adb操作',
      '.程序集变量 lsc, 雷神lsconsole操作',
      '.程序集变量 全_任务索引, 整数型',
      '',
      '.子程序 _按钮_启动_被单击',
      '',
      "' ① UI 线程只负责起线程与刷新界面，立刻返回",
      '全_任务索引＝0',
      '.如果真 (启动线程 (&线程_启动并连接, 0, ) ＝ 0)',
      '    信息框 (“线程创建失败”, 0, , )',
      '    返回 ()',
      '.如果真结束',
      '标签_状态.标题＝“正在启动中…”',
      '',
      '',
      '.子程序 线程_启动并连接',
      '.参数 线程参数, 整数型',
      '.局部变量 安装目录, 文本型',
      '.局部变量 结果, 文本型',
      '',
      "' ② 子线程里不要直接读写窗口组件，只做纯数据操作",
      '.如果真 (雷神_取运行中模拟器目录 (安装目录) ＝ 假)',
      '    安装目录＝雷神_取安装目录 (#版本_自动获取, 假)',
      '.如果真结束',
      '.如果真 (安装目录 ＝ “”)',
      '    输出调试文本 (“未找到模拟器安装目录”)',
      '    返回 ()',
      '.如果真结束',
      '',
      'adb.指定adb (安装目录 + “adb.exe”)',
      'lsc.置模拟器目录 (安装目录)',
      '',
      "' ③ 启动模拟器：第 3 个参数单位是【秒】",
      '.如果真 (lsc.启动模拟器 (全_任务索引, , 120) ＝ 假)',
      '    输出调试文本 (“启动失败或超时”)',
      '    返回 ()',
      '.如果真结束',
      '',
      "' ④ 等待连接：第 2 个参数单位是【毫秒】",
      '结果＝adb.等待连接 (全_任务索引, 60000)',
      '输出调试文本 (选择 (结果 ＝ “”, “已就绪”, “连接失败：” + 结果))'
    ].join('\n'), '启动线程完成整条流程'),

    '<h3>2. 界面更新的正确处理方式</h3>',
    '<ul>',
    '<li><b>不要</b>在子线程里直接设置 <code class="inline">标签.标题</code>、' +
    '<code class="inline">编辑框.内容</code> 等组件属性。</li>',
    '<li>推荐用“全局变量 + 定时器”：子线程只写全局变量，UI 线程用' +
    '<code class="inline">时钟_周期事件</code> 轮询刷新，改动最小、最不容易踩坑。</li>',
    '<li>需要精确回传时，可参考模块自带的 <code class="inline">窗口_模块调试</code>：' +
    '它用<b>子类化 + 自定义消息</b>把子线程日志投递到窗口线程，再统一刷到界面。</li>',
    '<li><b>不要</b>用 <code class="inline">处理事件 ()</code> 充当“假多线程”——' +
    '阻塞命令依然占着线程，界面只是看起来响应，操作很快会堆积。</li>',
    '</ul>',
    '<div class="warn"><b>类实例的线程归属：</b>每个线程请使用<b>自己的类实例</b>，' +
    '不要让多个线程同时调用同一个 <code class="inline">雷神adb操作</code> 对象。' +
    '虽然模块内的进程/管道调用本身是线程安全的，但同一个 adb 客户端实例、' +
    '同一个连接端口上的并发命令会互相污染输出（例如 A 命令读到 B 命令的结果）。</div>',

    '<h2>二、多线程共享数据：类_高性能读写锁</h2>',
    '<p>该锁的定位是“<b>同时读、独占写</b>”，适合多读少写场景；' +
    '只使用写锁部分时，它等价于一个高性能许可区。</p>',
    '<h3>1. 必须遵守的配对规则（源码“防呆说明”）</h3>',
    '<table>',
    '<tr><th style="width:34%">规则</th><th>说明</th></tr>',
    '<tr><td>进入读锁 → 退出读锁</td><td>必须成对。未退出读锁前，同一线程同一锁对象' +
    '<b>只能再次进入读锁</b>。</td></tr>',
    '<tr><td>进入写锁 → 退出写锁</td><td>必须成对。写锁持有期间，同一线程同一锁对象' +
    '可以再次进入写锁<b>或</b>读锁，但同样要逐层退出。</td></tr>',
    '<tr><td>读锁中不可再进入写锁</td><td><b>这是最容易踩的坑</b>：同一线程同一个锁对象，' +
    '在进入读锁后、退出读锁前调用进入写锁，会直接<b>死锁</b>。</td></tr>',
    '<tr><td>尝试进入读锁 / 写锁</td><td>非阻塞版本，返回真表示进入成功。' +
    '<b>成功之后同样必须配对退出</b>。</td></tr>',
    '</table>',
    '<h3>2. 读写分离的共享缓存</h3>',
    EPL.block([
      '.程序集变量 全_数据锁, 类_高性能读写锁',
      '.程序集变量 全_缓存表, 类_哈希表',
      '',
      '.子程序 写入缓存',
      '.参数 键, 文本型',
      '.参数 值, 文本型',
      '',
      '全_数据锁.进入写锁 ()',
      "' 第 3 个参数为真：键已存在时不覆盖，返回 3",
      '全_缓存表.添加 (键, 值, 真)',
      '全_数据锁.退出写锁 ()',
      '',
      '',
      '.子程序 读取缓存, 文本型',
      '.参数 键, 文本型',
      '.局部变量 局_结果, 文本型',
      '',
      "' 非阻塞抢锁：抢不到就走降级逻辑，避免线程被挂住",
      '.如果真 (全_数据锁.尝试进入读锁 ())',
      '    局_结果＝全_缓存表.取值 (键)',
      '    全_数据锁.退出读锁 ()',
      '.否则',
      '    局_结果＝“”',
      '.如果真结束',
      '返回 (局_结果)'
    ].join('\n'), '读写锁 + 哈希表做共享缓存'),

    '<h2>三、进程管道：类_命名管道 与 类_匿名管道</h2>',
    '<h3>1. 选型对照</h3>',
    '<table>',
    '<tr><th style="width:22%">对比项</th><th style="width:39%">类_命名管道</th><th>类_匿名管道</th></tr>',
    '<tr><td>底层实现</td><td>命名管道 + 重叠（异步）I/O</td><td>匿名管道 + PeekNamedPipe 轮询</td></tr>',
    '<tr><td>stderr 分流</td><td>读 () 有“是否错误输出”参数，可单独取错误流</td>' +
    '<td>无该参数，stdout/stderr 合并</td></tr>',
    '<tr><td>多命令并发</td><td>实例间互不干扰，更适合长连接交互</td><td>轮询方式，并发开销略高</td></tr>',
    '<tr><td>取剩余字数</td><td>取剩余字数 (是否错误输出)</td><td>取剩余字数 ()</td></tr>',
    '<tr><td>适用场景</td><td>长时间交互式会话、需区分错误输出</td><td>简单的一次性命令</td></tr>',
    '</table>',
    '<p>模块的终端层默认走哪一条，由 <code class="inline">置终端全局设置 ()</code> 的' +
    '“是否命名管道”决定，可用 <code class="inline">取默认命名管道 ()</code> 查询。</p>',

    '<h3>2. 正确调用顺序</h3>',
    '<p>管道类有严格的顺序要求：<b>启动 → 写 → 读 → 取退出代码 → 停止</b>。' +
    '<code class="inline">停止 ()</code> 会用 Job Object 终止<b>整棵进程树</b>，' +
    '随后关闭全部管道与进程句柄，并把进程 ID、线程 ID 清零——' +
    '所以<b>停止之后再取退出代码或进程 ID，拿到的都是无效值</b>。</p>',
    EPL.block([
      '.局部变量 管道, 类_命名管道',
      '.局部变量 编码转换, 类_编码转换',
      '.局部变量 数据, 字节集',
      '',
      "' ① 启动：返回 0 表示失败；有回调线程时返回线程句柄，无回调返回 1",
      '.如果真 (管道.启动 (“cmd.exe”, , , ) ＝ 0)',
      '    输出调试文本 (“管道启动失败”)',
      '    返回 ()',
      '.如果真结束',
      '',
      "' ② 写命令：要执行命令行，结尾必须带一个换行符",
      '管道.写 (“adb devices” + #换行符)',
      '',
      "' ③ 读取 stdout：超时 ≤ 0 会一直等到“读到数据且暂无后续数据”才返回",
      '数据＝管道.读 (, 15000, 假)',
      '输出调试文本 (编码转换.UTF8到文本 (数据))',
      '',
      "' ④ stdout 读完后，可再把 stderr 单独读出来（这是命名管道的优势）",
      '数据＝管道.读 (, 1000, 真)',
      '.如果真 (取字节集长度 (数据) > 0)',
      '    输出调试文本 (“错误输出：” + 编码转换.UTF8到文本 (数据))',
      '.如果真结束',
      '',
      "' ⑤ 先取退出代码，再停止；顺序反了就取不到值",
      '输出调试文本 (“退出代码：” + 到文本 (管道.取退出代码 ()))',
      '管道.停止 ()'
    ].join('\n'), '管道类的标准调用顺序'),
    '<div class="tip"><b>要点：</b><code class="inline">读 ()</code> 在没有数据时会阻塞。' +
    '如果是在 UI 线程或不确定输出长度的场景使用，<b>一定要传超时时间</b>；' +
    '另外管道实例不要跨线程共享，每个线程用各自的实例。</div>',

    '<h2>四、终端层调优</h2>',
    '<h3>1. 一次性设定全局策略</h3>',
    '<p>模块所有命令行调用都经过统一的“终端”入口，程序启动时设置一次即可全局生效：</p>',
    EPL.block([
      '.子程序 _启动窗口_创建完毕',
      '',
      "' 参数依次为：超时时间(毫秒)、默认终端、是否命名管道、调试输出",
      "' 全部参数均可空，留空表示保持原值不变",
      '置终端全局设置 (30000, 取终端路径 (“cmd.exe”), 真, 假)',
      '',
      "' 取回当前配置做确认",
      '输出调试文本 (“全局超时：” + 到文本 (取终端全局超时 ()))',
      '输出调试文本 (“默认终端：” + 取默认终端 ())',
      '输出调试文本 (“命名管道：” + 到文本 (取默认命名管道 ()))'
    ].join('\n'), '终端全局设置'),
    '<div class="warn"><b>超时时间的两种语义不要混淆：</b><br>' +
    '• <code class="inline">自定义命令 ()</code> 等接口的“超时时间”<b>留空</b>时' +
    '取全局超时；<br>' +
    '• <code class="inline">程序集_终端操作.取终端返回 ()</code> 的“超时时间”：' +
    '<b≥ 0</b> 取全局超时，<b>&gt; 0</b> 到时强制结束子进程，<b>&lt; 0</b> 一直等待执行完毕。</div>',

    '<h3>2. 即时回显：把长命令的输出流式打到日志</h3>',
    '<p>“即时回显”是一个子程序指针，回调在<b>子线程</b>中被调用，因此回调里不要操作窗口组件。' +
    '注意两个版本的参数类型不同：</p>',
    '<table>',
    '<tr><th style="width:34%">接口</th><th>回调子程序签名</th></tr>',
    '<tr><td>取终端返回 () / 取DOS返回 ()</td>' +
    '<td>一个<b>文本型</b>参数（回显文本），返回<b>逻辑型</b>：真=停止执行，假=继续</td></tr>',
    '<tr><td>取终端返回_字节集 () 系列</td>' +
    '<td>一个<b>整数型</b>参数（字节集指针），返回<b>逻辑型</b>：真=停止，假=继续</td></tr>',
    '</table>',
    EPL.block([
      '.子程序 日志回显, 逻辑型',
      '.参数 回显文本, 文本型',
      '',
      "' 回调在子线程执行：只写日志，不碰窗口组件",
      '输出调试文本 (“[实时] ” + 回显文本)',
      '返回 (假)                 ' + "' 返回假＝继续执行",
      '',
      '',
      '.子程序 执行长命令',
      '.局部变量 错误信息, 文本型',
      '',
      "' 参数依次为：命令行、超时、即时回显、当前目录、执行终端、错误信息、是否命名管道",
      '输出调试文本 (取终端返回 (“ping 127.0.0.1 -n 5”, 60000, &日志回显, , , 错误信息, 真))',
      '.如果真 (错误信息 ≠ “”)',
      '    输出调试文本 (“stderr：” + 错误信息)',
      '.如果真结束'
    ].join('\n'), '即时回显用法'),

    '<h2>五、自定义命令：突破封装边界</h2>',
    '<p>四个核心类都提供 <code class="inline">自定义命令 ()</code> 与 ' +
    '<code class="inline">自定义命令_字节集 ()</code>，可直接下发底层命令行，' +
    '用于“模块还没封装”或“排查到底是模块问题还是模拟器问题”的场景。</p>',
    EPL.block([
      '.局部变量 结果, 文本型',
      '.局部变量 错误信息, 文本型',
      '.局部变量 原始数据, 字节集',
      '',
      "' 参数依次为：命令文本、连接端口、工作目录、返回数据、超时时间、错误信息、执行终端",
      '结果＝adb.自定义命令 (“shell wm size”, , , , 15000, 错误信息)',
      '.如果真 (结果 ＝ “” 且 错误信息 ≠ “”)',
      '    输出调试文本 (“执行失败：” + 错误信息)',
      '.如果真结束',
      '输出调试文本 (结果)',
      '',
      "' 需要原始字节集时（避免编码二次转换），用带 _字节集 的版本",
      '原始数据＝adb.自定义命令_字节集 (到字节集 (“shell getprop”))',
      '输出调试文本 (到文本 (原始数据))'
    ].join('\n'), '自定义命令'),

    '<h3>批量下发多条 shell 语句</h3>',
    '<p><code class="inline">自定义命令_管理员 ()</code> 的“命令文本”参数是' +
    '<b>文本型数组</b>，一次调用即可执行多条语句，比循环调用 <code class="inline">shell ()</code> ' +
    '少创建 N 次进程。第 2 个参数位置在各类中不同，务必按当前类的签名传参。</p>',
    EPL.block([
      '.局部变量 命令数组, 文本型, , "0"',
      '.局部变量 结果, 文本型',
      '',
      '加入成员 (命令数组, “chmod 755 /system/bin/test”)',
      '加入成员 (命令数组, “ls -l /system/bin/test”)',
      '',
      "' adb 类：索引, 命令数组, 工作目录, 返回数据, 超时, 错误信息, 执行终端, 遇错停止",
      '结果＝adb.自定义命令_管理员 (0, 命令数组, , , 15000, , , 真)',
      '输出调试文本 (结果)',
      '',
      "' lsconsole 类：索引, 命令数组, 返回数据, 超时, 错误信息, 遇错停止",
      '结果＝lsc.自定义命令_管理员 (0, 命令数组, , 15000, , 真)',
      '输出调试文本 (结果)'
    ].join('\n'), '一次下发多条 shell 语句'),
    '<div class="danger"><b>转义要求：</b>批量语句中的特殊字符必须按 cmd 规则转义——' +
    '例如半角双引号要写成三个双引号。另外该命令需要模拟器已开启 ROOT，' +
    '否则会提示 <code class="inline">/system/bin/sh: su: not found</code>。</div>',

    '<h2>六、批量与索引技巧：一次调用代替 N 次</h2>',
    '<ul>',
    '<li><b>MuMu 支持一次操作多个实例：</b>索引参数是“通用型（参考）”，需先放进变量再传入；' +
    '写 <code class="inline">“0,2,4”</code> 表示同时操作这三个，写 ' +
    '<code class="inline">“all”</code> 或留空表示<b>全部</b>——' +
    '所以“只想启动 0 号”时一定要显式传入 <code class="inline">“0”</code>。</li>',
    '<li><b>一次性取回全部信息</b>，而不是循环单取：' +
    '<code class="inline">lsc.取全部模拟器EX ()</code>、' +
    '<code class="inline">lsc.取全部模拟器信息 ()</code>、' +
    '<code class="inline">lsc.取模拟器信息EX2 ()</code>、' +
    '<code class="inline">mu.取信息 ()</code>；其中源码特别注明' +
    '“模拟器数量较多时，推荐使用 <code class="inline">取模拟器信息EX2 ()</code>”。</li>',
    '<li><b>缓存不变的值：</b>安装目录、adb 路径、模拟器端口在程序运行期间基本不变，' +
    '取一次存到程序集变量里复用，避免每次操作都重新探测注册表。</li>',
    '<li><b>合并文件操作：</b>需要连续上传多个文件时，直接上传整个目录' +
    '（<code class="inline">上传文件 ()</code> 支持目录），优于逐个文件调用。</li>',
    '<li><b>批量安装：</b><code class="inline">批量安装应用 ()</code> 只需指定存放 apk 的目录。</li>',
    '</ul>',
    EPL.block([
      '.局部变量 索引, 文本型',
      '.局部变量 结果, 文本型',
      '',
      "' MuMu 批量：一次让 0/2/4 号模拟器同时启动",
      '索引＝“0,2,4”',
      '结果＝mu.启动 (索引, )',
      '输出调试文本 (结果)',
      '',
      "' 只要 0 号：必须显式写 “0”，留空会被当成 all",
      '索引＝“0”',
      '结果＝mu.关闭 (索引)'
    ].join('\n'), 'MuMu 批量索引'),

    '<h2>七、容器类参数取舍：类_哈希表 / 类_数组 / 类_队列</h2>',
    '<h3>1. 类_哈希表 的创建参数</h3>',
    '<table>',
    '<tr><th style="width:24%">参数</th><th style="width:30%">默认值</th><th>取舍建议</th></tr>',
    '<tr><td>参_键类型</td><td>整数型（3）</td><td>可用 3=整数型、4=长整数型、10=文本型、11=字节集型</td></tr>',
    '<tr><td>参_值类型</td><td>整数型（3）</td><td>额外支持 5=小数型、6=双精度小数型、7=逻辑型、99=自定义数据</td></tr>',
    '<tr><td>参_容量</td><td>16</td><td>已知规模时一次给足（如预计 1 万条就给 16384），可显著减少扩容重排</td></tr>',
    '<tr><td>参_负载因子</td><td>0.75</td><td>调大省内存、调小查得快，一般保持默认</td></tr>',
    '<tr><td>参_是否减容</td><td>真</td><td>删除后容量超过总数 8 倍会自动缩容；追求极致速度可设为假</td></tr>',
    '<tr><td>参_不使用内存池</td><td>假</td><td><b>用内存池换性能，不用内存池换内存</b>——高频增删保留默认，长时间驻留的大表可设真</td></tr>',
    '<tr><td>参_有序模式</td><td>假</td><td>为真时 <code class="inline">取所有键 ()</code>/<code class="inline">取所有值 ()</code> ' +
    '按添加顺序返回，且 <code class="inline">有序_</code> 系列方法才可用</td></tr>',
    '</table>',
    '<div class="warn"><b>传参限制：</b>“参_键 / 参_值”是<b>通用型（参考）</b>，' +
    '数值类型不能直接写常量（双精度小数除外）——<code class="inline">添加 (1, 2)</code> ' +
    '会被当成双精度小数传入。解决办法是把数值放进对应类型的变量后再传。</div>',
    EPL.block([
      '.局部变量 表, 类_哈希表',
      '.局部变量 键, 文本型',
      '.局部变量 值, 文本型',
      '.局部变量 局_返回, 整数型',
      '.局部变量 局_键数组, 文本型, , "0"',
      '',
      "' 10=键为文本型，10=值也是文本型；容量 4096；有序模式=真",
      '.如果真 (表.创建 (10, 10, 4096, , , , 真) ＝ 假)',
      '    输出调试文本 (“建表失败”)',
      '    返回 ()',
      '.如果真结束',
      '',
      '键＝“user_1001”',
      '值＝“张三”',
      '',
      "' 返回值：0=失败，1=首次添加，2=覆盖，3=键已存在且未覆盖",
      '局_返回＝表.添加 (键, 值, 真)',
      '输出调试文本 (“添加返回：” + 到文本 (局_返回))',
      '',
      "' 有序模式下按添加顺序取回全部键",
      '输出调试文本 (“键总数：” + 到文本 (表.取所有键 (局_键数组, )))',
      '',
      "' 用完清空：清空后必须重新创建才能继续使用",
      '表.清空 ()'
    ].join('\n'), '类_哈希表用法'),

    '<h3>2. 类_数组</h3>',
    '<ul>',
    '<li><b>只支持文本型与字节集型</b>（类型 10 / 11），其他类型请用易语言自带数组。</li>',
    '<li><code class="inline">是否哈希＝真</code> 会在添加时计算哈希，' +
    '<b>适合大数据量下的查询与去重</b>，代价是添加与修改稍慢。</li>',
    '<li>批量搬数据优先用 <code class="inline">连续添加 / 连续取值 / 连续插入 / 连续删除</code>，' +
    '比循环单条调用少一层调用开销。</li>',
    '<li><code class="inline">清空 ()</code> 后必须重新 <code class="inline">创建 ()</code>。</li>',
    '</ul>',
    EPL.block([
      '.局部变量 数组, 类_数组',
      '.局部变量 源数组, 文本型, , "0"',
      '.局部变量 取出数组, 文本型, , "0"',
      '',
      "' 10=文本型；初始容量 1024；开哈希便于查询与去重",
      '数组.创建 (10, 1024, 真)',
      '',
      "' 批量灌入：把易语言数组整段搬进类数组，返回总成员数",
      '数组.连续添加 (源数组, 1, 取数组成员数 (源数组))',
      '输出调试文本 (“成员数：” + 到文本 (数组.取成员数 ()))',
      '',
      "' 去重：返回去重后的成员数量",
      '输出调试文本 (“去重后：” + 到文本 (数组.去重复 ()))',
      '',
      "' 查询与匹配（哈希模式下更快）",
      '输出调试文本 (到文本 (数组.查询 (“需要查找的文本”)))',
      ''
    ].join('\n'), '类_数组用法'),

    '<h3>3. 类_队列</h3>',
    '<p><code class="inline">创建 ()</code> 返回<b>整数型</b>：成功返回 1，失败返回 0。' +
    '第 3 个参数 <code class="inline">是否设置哈希值＝真</code> 时，' +
    '元素在压入时会附带哈希，便于快速查询，代价是压入稍慢。</p>',
    EPL.block([
      '.局部变量 队列, 类_队列',
      '.局部变量 数据, 文本型',
      '',
      "' 参数：队列类型、方向(真=尾进头出)、是否设置哈希值",
      '.如果真 (队列.创建 (10, 真, 假) ≠ 1)',
      '    输出调试文本 (“队列创建失败”)',
      '    返回 ()',
      '.如果真结束',
      '',
      '数据＝“第一条任务”',
      '队列.压入 (数据)',
      '输出调试文本 (“队列长度：” + 到文本 (队列.取队列数量 ()))'
    ].join('\n'), '类_队列用法'),

    '<h2>八、编码与文本处理</h2>',
    '<p><code class="inline">类_编码转换</code> 提供 ANSI / UTF-16 / UTF-8 三者之间的互转，' +
    '每个方向都有三个变体：<b>文本版</b>（直接进出文本）、<b>字节集版</b>（带结束符开关）、' +
    '<b>指针版</b>（直接操作内存指针，适合高频场景）。别名与常见用途：</p>',
    '<table>',
    '<tr><th style="width:32%">方向</th><th style="width:22%">别名</th><th>典型用途</th></tr>',
    '<tr><td>文本到宽文本</td><td>A2W</td><td>命令行参数、Windows API 宽字符参数</td></tr>',
    '<tr><td>宽文本到文本</td><td>W2A</td><td>把宽字符结果转回易语言文本型</td></tr>',
    '<tr><td>文本到UTF8 / UTF8到文本</td><td>A2U / U2A</td><td>写 UTF-8 文件、解析 adb 的 UTF-8 输出</td></tr>',
    '<tr><td>宽文本到UTF8 / UTF8到宽文本</td><td>W2U / U2W</td><td>两种 Unicode 表示之间的桥接</td></tr>',
    '</table>',
    EPL.block([
      '.局部变量 编码转换, 类_编码转换',
      '.局部变量 原始数据, 字节集',
      '',
      "' ① 模块的 _字节集 版接口返回的通常是 UTF-8，需显式解码",
      '原始数据＝adb.自定义命令_字节集 (到字节集 (“shell getprop ro.product.model”))',
      '输出调试文本 (编码转换.UTF8到文本 (原始数据))',
      '',
      "' ② 写出 UTF-8 文件：第 2 参数=真 表示去掉结尾的 0 结束符",
      '写到文件 (“E:\\out.txt”, 编码转换.文本到UTF8 (“中文内容”, 真))',
      '',
      "' ③ 高频循环里可关闭返回时的内存收缩以换取速度（省内存则保持默认）",
      '编码转换.内存收缩设置 (假)'
    ].join('\n'), '编码转换'),
    '<div class="tip"><b>提示：</b><code class="inline">读文本 ()</code> 返回乱码时，' +
    '优先怀疑文件本身不是 ANSI 编码；' +
    '另外 <code class="inline">编码_DOS转义 ()</code> / <code class="inline">解码_DOS转义 ()</code> ' +
    '可用于命令行中的特殊字符处理。</div>',

    '<h2>九、截图与图像数据流</h2>',
    '<table>',
    '<tr><th style="width:30%">方式</th><th>特点与适用场景</th></tr>',
    '<tr><td>adb.截屏 (索引, 文件路径)</td><td>直接存到电脑文件，最省内存，适合低频截图留档</td></tr>',
    '<tr><td>adb.截屏_字节集 (索引)</td><td>返回图片字节集，可二次处理；源码注明 MuMu 不推荐</td></tr>',
    '<tr><td>mu.增强_截图 (句柄, 标识, 左, 顶, 宽, 高)</td>' +
    '<td>渲染层直连，返回 <b>BMP 字节集</b>，适合高频抓图；' +
    '尺寸是<b>模拟器分辨率</b>而非窗口尺寸，可只截指定区域以降低数据量</td></tr>',
    '</table>',
    EPL.block([
      '.局部变量 句柄, 整数型',
      '.局部变量 标识, 整数型',
      '.局部变量 图片, 字节集',
      '',
      "' 高频抓图：只截 (0,0) 起 400x300 的区域，数据量比整屏小得多",
      '句柄＝mu.增强_连接 (“0”)',
      '.如果真 (句柄 ≤ 0)',
      '    返回 ()',
      '.如果真结束',
      '标识＝mu.增强_取显示标识 (句柄, “com.tencent.mm”, )',
      '.如果真 (标识 ≥ 0)',
      '    图片＝mu.增强_截图 (句柄, 标识, 0, 0, 400, 300)',
      '    写到文件 (取运行目录 () + “\\cut.bmp”, 图片)',
      '.如果真结束',
      'mu.增强_断开 (句柄)'
    ].join('\n'), 'MuMu 增强截图（区域截取）'),

    '<h2>十、性能与稳定性清单</h2>',
    '<h3>1. 性能优化要点</h3>',
    '<ol>',
    '<li><b>减少进程创建次数</b>：能用一条命令解决的不要拆成多条；' +
    '多条 shell 语句用 <code class="inline">自定义命令_管理员 ()</code> 批量下发。</li>',
    '<li><b>复用类实例</b>：类变量声明在程序集级别，整个程序生命周期只创建一次。</li>',
    '<li><b>统一超时策略</b>：用 <code class="inline">置终端全局设置 ()</code> 设一个合理的默认值，' +
    '个别慢命令再单独放大，避免到处都是无限等待。</li>',
    '<li><b>避免重复探测</b>：安装目录、adb 路径、连接端口取一次后缓存。</li>',
    '<li><b>容器预设容量</b>：<code class="inline">类_哈希表</code> 与 <code class="inline">类_数组</code> ' +
    '都支持预设容量，按预估规模一次给足。</li>',
    '<li><b>开启加速优化</b>：程序入口调用一次 ' +
    '<code class="inline">程序_加速优化 (真)</code>，可提升 DLL 调用与内存分配释放速度' +
    '（重复调用无副作用）。</li>',
    '<li><b>精简截图数据量</b>：只截需要的区域，或降低抓图频率。</li>',
    '</ol>',

    '<h3>2. 资源回收清单</h3>',
    '<table>',
    '<tr><th style="width:36%">对象</th><th>必须做的收尾动作</th></tr>',
    '<tr><td>类_哈希表 / 类_数组 / 类_队列</td>' +
    '<td>调用 <code class="inline">清空 ()</code> 释放内存；<b>清空后需重新创建</b>才能继续使用</td></tr>',
    '<tr><td>类_命名管道 / 类_匿名管道</td>' +
    '<td>调用 <code class="inline">停止 ()</code>；它会终止整棵进程树并关闭全部句柄</td></tr>',
    '<tr><td>MuMu 增强系列</td>' +
    '<td><code class="inline">增强_连接 ()</code> 必须与 <code class="inline">增强_断开 ()</code> 配对，' +
    '否则连接句柄泄漏</td></tr>',
    '<tr><td>自定义服务 / CE 服务端</td>' +
    '<td>用 <code class="inline">自定义服务_停止 (进程ID)</code>、' +
    '<code class="inline">CE服务端_停止 (进程ID)</code> 结束</td></tr>',
    '<tr><td>类_高性能读写锁</td>' +
    '<td>每次成功进入读锁/写锁，都要在<b>所有分支</b>（含提前返回）配对退出</td></tr>',
    '<tr><td>正则表达式类</td><td>需自行初始化 COM 库，并在同一线程内使用</td></tr>',
    '</table>',

    '<h3>3. 顺序敏感的命令</h3>',
    '<ul>',
    '<li><code class="inline">lsc.安装应用 ()</code> 是<b>异步</b>的，调用后立即返回；' +
    '紧随其后的启动应用可能失败。需要严格同步请改用 <code class="inline">adb.安装应用 ()</code>。</li>',
    '<li><code class="inline">修改配置 ()</code>、<code class="inline">雷神_修改配置 ()</code>、' +
    '<code class="inline">雷神_设置窗口位置和尺寸 ()</code> 都要在<b>模拟器关闭状态</b>下改，' +
    '下次启动才生效。</li>',
    '<li><b>先 ROOT、再重载文件系统、最后写文件</b>：顺序颠倒会导致写入只读分区失败。</li>',
    '<li>删除用户等操作存在异步清理过程（源码已注明），期间 adb 命令可能失败——' +
    '建议轮询状态而不是立即重试。</li>',
    '</ul>',

    '<div class="ok"><b>相关章节：</b>错误对照与排错请见 ' +
    '<a href="#/faq">常见问题解答</a>；参数与返回值细节请见 ' +
    '<a href="#/api:%E9%9B%B7%E7%A5%9Eadb%E6%93%8D%E4%BD%9C">接口文档</a>。</div>'
  ].join('\n')
});
